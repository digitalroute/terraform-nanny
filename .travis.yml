sudo: false
services:
  - docker
install:
  - docker build -t terraform-nanny:local .
before_script:
  - cd example/tf-10-region && docker run -v `pwd`:/src --entrypoint "cd /src && terraform workspace new production && terraform workspace new test" terraform-nanny:local
script:
  - cd example && docker run -v `pwd`:/src terraform-nanny:local
